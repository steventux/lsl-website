<div class="block">
  <div class="secondary-navigation">
    <ul class="wat-cf">
      <li class="first"><%= link_to pat(:list), url(:contents, :index) %></li>
      <li class="active"><%= link_to pat(:new), url(:contents, :new) %></li>
    </ul>
  </div>
  <div class="content">
    <h2 class="title"><%= pat(:new) %> <% mt(:content) %></h2>
    <div class="inner">
      <% form_for :content, url(:contents, :create), :class => :form do |f| %>
        <%= partial "contents/form", :locals => { :f => f } %>
      <% end %>
    </div>
  </div>
</div>
<% content_for :sidebar, partial("base/sidebar") %>
